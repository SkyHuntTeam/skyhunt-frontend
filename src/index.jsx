import {Login} from '../Login';
import React from 'react';
import { ReactDOM } from "react";
import App from './App';
import Register from '../Register';


  
  const root = ReactDOM.createRoot(document.gerElementById('root'));
  root.render(
    
        <App />
  )